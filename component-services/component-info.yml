service:
  service_name: component-service
  service_prefix: component
  service_available: true
  service_description: Backend para el manejo de componentes
  service_core: true
  service_wait: true

#Aca se definen los roles y permisos del servicio
roles:
  admin-componentes:
    - component.control.investigacion
    - component.control.servicios
    - component.control.servicios_parada
    - component.control.redireccion
    
    